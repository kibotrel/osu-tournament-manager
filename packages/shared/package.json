{
  "devDependencies": {
    "@packages/prettier-config": "workspace:*",
    "@types/express": "4.17.21",
    "jsdom": "27.0.0",
    "vitest": "3.2.4"
  },
  "imports": {
    "#src/*": "./src/*"
  },
  "main": "dist/src/sharedExport.js",
  "name": "@packages/shared",
  "private": true,
  "scripts": {
    "build": "tsc && tsc-alias",
    "dev": "nodemon --config nodemon.json",
    "format": "prettier --write src/**/*.ts",
    "lint": "eslint src/**/*.ts --config .eslintrc",
    "test:unit": "vitest --silent --config vitest.config.unit.ts",
    "type-check": "tsc --noEmit"
  },
  "type": "module",
  "types": "dist/src/sharedExport.d.ts"
}
